// Libraries
import React from 'react';

// Component

// Style

// Types


interface Props {
  id: number;
}

const SubmissionsTable: React.FC<Props> = (props) => {
  const { id } = props;
  return (
    <div>

    </div>
  );
};

export default SubmissionsTable;