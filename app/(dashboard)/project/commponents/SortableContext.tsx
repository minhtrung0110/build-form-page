// Libraries
import React from 'react';

// Component

// Style

// Types


interface Props {
  // Define your component's props here
}

const SortableContext: React.FC<Props> = (props) => {
  return <Context.Provider value={contextValue}>{children}</Context.Provider>;

};

export default SortableContext;